# Production Environment Variables for Backend
# NEVER commit this file with real values!
# Copy to .env and fill in production secrets

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=3333
LOG_LEVEL=info

# ===========================================
# SECURITY (REQUIRED - Generate unique values!)
# ===========================================
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_64

# Generate with: openssl rand -base64 32
TURN_SHARED_SECRET=CHANGE_ME_generate_with_openssl_rand_base64_32

# ===========================================
# DATABASE (REQUIRED)
# ===========================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://paircam:password@localhost:5432/paircam?sslmode=require

# ===========================================
# REDIS (REQUIRED)
# ===========================================
# Option 1: Redis URL (recommended for cloud Redis like Upstash)
REDIS_URL=redis://default:password@your-redis-host:6379

# Option 2: Separate host/port (fallback if URL not set)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# ===========================================
# TURN SERVER (REQUIRED for WebRTC)
# ===========================================
TURN_HOST=turn.paircam.live
TURN_PORT=3478
TURN_REALM=paircam.live

# ===========================================
# CORS (REQUIRED)
# ===========================================
# Comma-separated list of allowed origins
CORS_ORIGINS=https://paircam.live,https://www.paircam.live

# ===========================================
# STRIPE PAYMENTS (REQUIRED for premium)
# ===========================================
# Get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_CHANGE_ME
STRIPE_WEBHOOK_SECRET=whsec_CHANGE_ME

# Price IDs from Stripe Dashboard
STRIPE_PRICE_ID_WEEKLY=price_CHANGE_ME
STRIPE_PRICE_ID_MONTHLY=price_CHANGE_ME

# ===========================================
# FRONTEND URL (for Stripe redirects)
# ===========================================
FRONTEND_URL=https://paircam.live

# ===========================================
# OPTIONAL: OAuth (if implementing social login)
# ===========================================
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_CALLBACK_URL=https://api.livecam.app/auth/google/callback

# ===========================================
# OPTIONAL: Rate Limiting
# ===========================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# OPTIONAL: Monitoring
# ===========================================
# SENTRY_DSN=https://xxx@sentry.io/xxx
